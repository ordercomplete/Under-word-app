# Error Log — 2026-08-22

## 22:11 — Cline (ox-alpha, онлайн): перевірка команд sync-agent.py
**Помилка 1:** Команди з README (`python sync-agent.py ...`) падають з кореня репо: `can't open file ... sync-agent.py [Errno 2]`. Скрипт лежить у `scripts/`, а не в корені.
**Помилка 2 (критична):** Повторний запуск sync на існуючу ціль завжди падав: `PermissionError: [WinError 32]` у `BackupManager.create_backup` → `shutil.copy2`. Причина: `main()` передавав абсолютний шлях, а `self.backup_dir / absolute_path` повністю замінював базу → copy2 копіював файл сам у себе.
**Фікс:** `scripts/sync-agent.py` — обчислення відносного шляху через `relative_to(target_root)` + guard від self-copy. Перевірено: повторний sync EXIT=0, backup'и створюються правильно.