import React from "react";
import Modal from "react-modal";

Modal.setAppElement("#root");

const ModalChapterSelector = ({ lang }) => {
  return <Modal isOpen={false}>Placeholder for chapter selector</Modal>;
};

export default ModalChapterSelector;
