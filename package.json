{
  "name": "under-word-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "bootstrap": "^5.3.2",
    "bootstrap-icons": "^1.13.1",
    "fs": "^0.0.1-security",
    "path": "^0.12.7",
    "react": "^18.2.0",
    "react-bootstrap": "^2.10.0",
    "react-dom": "^18.2.0",
    "react-modal": "^3.16.1",
    "react-responsive": "^10.0.1",
    "react-router-dom": "^6.22.0",
    "react-table": "^7.8.0",
    "react-window": "^1.8.11",
    "styled-components": "^6.1.8"
  },
  "scripts": {
    "start": "react-scripts start",
    "start:with-api": "node server/index.js",
    "api": "node server/index.js",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "convert-json": "node scripts/convertTranslations.js",
    "convert-and-replace": "node scripts/convertTranslations.js && cp -r public/data_compressed/* public/data/",
    "build:with-data": "react-scripts build && cp -r public/data_compressed/* build/data/",
    "deploy": "npm run build:with-data && netlify deploy"
  },
  "proxy": "http://localhost:3000",
  "devDependencies": {
    "react-scripts": "5.0.1"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
