# Error log — 2026-08-26

### 🔴 UnicodeEncodeError у migrate-to-central-agent.py
**Час:** 2026-08-26 15:52
**Tool:** run_commands (`--collect`)
**Суть:** `print("📊 ...")` падав на cp1251-консолі — у скрипті був відсутній `sys.stdout.reconfigure(encoding='utf-8')`.
**Фікс:** додано type-safe reconfigure на початку main() + `import sys` (його теж не було — виявилось при першому запуску фіксу: NameError).
**Результат:** --collect → exit 0. ✅

### 🔴 install.ps1 не парситься PowerShell 5.1
**Час:** 2026-08-26 16:00
**Tool:** test_install.ps1
**Суть:** `.ps1` без UTF-8 BOM читається PS 5.1 як ANSI → кирилиця/emoji ламають парсер («Отсутствует закрывающий знак }»). pwsh не страждав.
**Фікс:** перекодовано install.ps1 та test_install.ps1 у UTF-8 з BOM ([System.IO.File]::WriteAllText + Encoding.UTF8).
**Результат:** Parser::ParseFile → 0 помилок; тест інсталяції запускається. ✅

### 🟡 Структурна проблема (не усунена — потрібен рішення користувача)
**Суть:** у git HEAD стара структура (scripts/, manifest.json у корені); нова `agent_config/` untracked. Через це `install.ps1` (git clone) підтягує старі шляхи і sync-крок E2E не працює.
**Рекомендація:** закомітити нову структуру (agent_config/, видалення scripts/) окремим коммітом.

---
